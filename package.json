{
  "name": "fiche-personnage-dark-fantasy",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "server": "node server.js",
    "build": "vite build",
    "preview": "vite preview",
    "cleanup:images": "node scripts/cleanup-unused-images.js",
    "cleanup:images:dry-run": "node scripts/cleanup-unused-images.js --dry-run"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "multer": "^2.0.2",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^5.0.0",
    "vite": "^7.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}
